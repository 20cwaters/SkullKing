# Only needed for local dev (client on Vite's port, server on its own) or if you split the
# client and server into separate deployed services. In the default single-service Render
# deploy, the client is served by the same origin as the server, so this isn't set at all.
VITE_SERVER_URL=http://localhost:4000
